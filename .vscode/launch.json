{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "attach",
            "port": 9229,
            "name": "Docker: Attach to Node",
            "localRoot": "${workspaceFolder}/server/",
            "remoteRoot": "/usr/server/",
        },
    ]
}